---
import { getEntry } from "astro:content";
import "../styles/reset.css" 
import "../styles/style.css" 
const entry = await getEntry("logs", "index");

if (entry === undefined) {
  return Astro.redirect("/404");
}

const { Content } = await entry.render();
---

<Content />
